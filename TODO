features:
 * Rt.tvar / Rt.tvar_ty
 * implement missing stuff in Vm
 * (() do end) -> do || end
 * store location in fir instructions
 * Array -> Vector
 * llvm_gen for vector instrinsics
 * x = 1 -> let mut x = 1
 * != -> !(==), <=/>=/</> -> <=>

bugs:
 * faster iv type lookup
