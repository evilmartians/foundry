features:
 * pass manager
 * value types
 * inliner
 * better errors
 * store location map in fir
 * name_of_value -> const

bugs:
 * allocate fresh tvars on copy_func
 * better value_env
 * better mangling
