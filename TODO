features:
 * != -> !(==)
 * (() do end) -> do || end
 * x = 1 -> let mut x = 1
 * inliner
 * better errors
 * store location map in fir
 * insert_instr
 * Rt.tvar / Rt.tvar_ty

bugs:
 * faster iv type lookup
