features:
 * HOF
 * value types
 * replace tvars in consts
 * inliner
 * better errors
 * store location map in fir
 * name_of_value -> const
 * x_foo_ty -> x_ty_foo

bugs:
 * allocate fresh tvars on copy_func
 * better value_env
 * better mangling
