features:
 * Rt.tvar / Rt.tvar_ty
 * (() do end) -> do || end
 * store location in fir instructions
 * llvm_gen for array instrinsics
 * x = 1 -> let mut x = 1
 * != -> !(==), <=/>=/</> -> <=>

bugs:
 * faster iv type lookup
