features:
 * integer coercion
 * != -> !(==)
 * (() do end) -> do || end
 * x = 1 -> let mut x = 1
 * inliner
 * better errors
 * store location map in fir
 * insert_instr

bugs:
 * allocate fresh tvars on copy_func
 * faster iv type lookup
