usb:
 * export
 * class and class
 * Memory, fat_alloc
 * Memory#fill
 * case/when
 * (value : ty)
 * [< >]

features:
 * common frontend
 * store location in fir instructions
 * (() do end) -> do || end
 * x = 1 -> let mut x = 1
 * != -> !(==), <=/>=/</> -> <=>

bugs:
 * faster iv type lookup
